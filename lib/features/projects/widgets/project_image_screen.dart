import 'package:flutter/material.dart';

class ProjectImagesScreen extends StatelessWidget {
  const ProjectImagesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(children: [Image.asset("name")]),
    );
  }
}
